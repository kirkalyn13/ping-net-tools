import { StyleSheet } from 'react-native'
import { COLOR_LIGHT_PRIMARY } from './Colors.styles';

const styles = StyleSheet.create({
    appContainer: {
      flex: 1,
      paddingTop: 50,
      paddingHorizontal: 16,
      backgroundColor: COLOR_LIGHT_PRIMARY,
    },
  });

export default styles