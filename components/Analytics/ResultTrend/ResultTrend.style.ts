import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
      title: {
        fontWeight: "bold",
        marginBottom: 8
      },
      infoContainer: {
        width: "100%",
        textAlign: "center",
        fontWeight: "bold",
        marginTop: 16
      }
  })

export default styles