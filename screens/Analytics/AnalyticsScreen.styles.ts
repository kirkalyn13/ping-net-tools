import { StyleSheet } from 'react-native'
import { COLOR_LIGHT_PRIMARY } from '../../styles/Colors.styles';

const styles = StyleSheet.create({
    container: {
        height: "100%",
        paddingHorizontal: 8,
        backgroundColor: COLOR_LIGHT_PRIMARY,
        overflow: "scroll"
    },
  });

export default styles